services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: ai-room-recommendation:local
    container_name: ai-room-recommendation-api
    init: true
    ports:
      - "8000:8000"
    restart: unless-stopped
